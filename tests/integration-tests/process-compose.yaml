
version: "0.5"

is_tui_disabled: true
is_dotenv_disabled: true
log_level: info

processes:
  vncache:
    command: task run --
      --threads 2 
      --verbose 
      --log-transport 
      --log-http 
      --input-off
    working_dir: ${SERVER_BUILD_DIR}
    availability:
      restart: "always"
      max_restarts: 5
    # probe against the open well-known endpoint
    readiness_probe:
      http_get:
        host: 127.0.0.1
        port: ${TEST_SERVER_PORT}
        path: /.well-known/vncache
      initial_delay_seconds: 3
      period_seconds: 3

  # run tests after vncache starts and is healthy, then run tests and exit
  tests:
    command: task run-tests
    availability:
      exit_on_termination: true
      restart: "no"
    depends_on:
      vncache:
        condition: process_healthy
